## Invite-only soldier resilience app with private journaling, squad groups (max 45 users), doctrine-informed content, and reporting

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
